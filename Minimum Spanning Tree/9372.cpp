#define _CRT_SECURE_NO_WARNINGS

#include <cstdio>

int main() {
	int T;
	scanf("%d", &T);

	for (int t = 0; t < T; ++t) {
		int N, M;
		scanf("%d %d", &N, &M);

		for (int m = 0; m < M; ++m) {
			int tempA, tempB;
			scanf("%d %d", &tempA, &tempB);
		}
		printf("%d\n", N - 1);
	}

	return 0;
}
